require 'spec_helper'

describe Video::Crawler do
  it 'has a version number' do
    expect(Video::Crawler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
